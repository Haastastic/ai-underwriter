# Copy to .env (git-ignored) and fill in. The backend loads .env at startup
# via app.backend.config.load_env_file(); real environment variables take
# precedence over anything set here.

# Anthropic API key. When unset, the backend falls back to an offline stub
# that emits a clearly-marked draft notice instead of calling a model.
ANTHROPIC_API_KEY=

# --- Optional backend overrides (defaults shown) ---
# AIU_MODELS_ROOT=models
# AIU_MODEL_VERSION=v2
# AIU_TRAINING_DATA=data/raw/cs-training.csv
# AIU_DB_PATH=underwriter.db
# AIU_LLM_MODEL=claude-haiku-4-5
# AIU_MAX_REASONS=4
# AIU_APPROVE_BELOW=0.08
# AIU_DENY_AT_OR_ABOVE=0.28
# A model version and its cutoffs go together -- each version's
# metadata.json records the cutoffs recommended for its calibration. To
# serve the original v1 baseline (age features included):
# AIU_MODEL_VERSION=v1
# AIU_APPROVE_BELOW=0.08
# AIU_DENY_AT_OR_ABOVE=0.30
# Comma-separated browser origins allowed to call the API (the loan-officer
# frontend). Defaults cover the Vite dev server; a hosted UI adds its origin.
# AIU_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
